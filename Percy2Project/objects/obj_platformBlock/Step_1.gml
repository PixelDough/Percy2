/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

//if place_meeting(x, bbox_top-1, obj_platformBlock) and depth >= instance_place(x, bbox_top-1, obj_platformBlock).depth
//if place_meeting(x, y, obj_platformBlock) and bbox_top > instance_place(x, y, obj_platformBlock).bbox_top and depth >= instance_place(x, y, obj_platformBlock).depth
//	depth--
	
//if place_meeting(x, y, obj_platformBlock) and bbox_left > instance_place(x, y, obj_platformBlock).bbox_left and depth >= instance_place(x, y, obj_platformBlock).depth
//	depth--
