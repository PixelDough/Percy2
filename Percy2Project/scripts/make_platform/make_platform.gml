///@param target
var _target = argument0;
with all {
	if object_index == _target or object_is_ancestor(self.object_index, _target) {
		if other.y > bbox_top
			solid_ = false;
		else
			solid_ = true;
	}
}